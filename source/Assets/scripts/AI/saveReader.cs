using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class saveReader : MonoBehaviour { //This class is for reading the save file of the population and giving the current population its initial organisms from it

    //parses the save file to get the population and the generation number
    public float[][] parseSave() {
        return new float[99][];
    }

	// Use this for initialization
	void Start () {
        
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
